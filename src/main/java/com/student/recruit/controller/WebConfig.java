package com.student.recruit.controller;

import com.student.recruit.config.Swagger2Config;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Import;
import org.springframework.web.servlet.config.annotation.EnableWebMvc;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurerAdapter;

/**
 * Created by xiachao on 2017/3/9.
 * ")
 * .addResourceLocations("classpath:/templates/dist/webjars/");
 * }
 * }
 */
/*
@Configuration
@EnableWebMvc
@ComponentScan(basePackageClasses = ApiController.class)
@Import(Swagger2Config.class)
public class WebConfig extends WebMvcConfigurerAdapter {

    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        registry
                .addResourceHandler("swagger-ui.html")
                .addResourceLocations("classpath:/templates/dist/");
        registry
                .addResourceHandler("/webjars*/
/**")
 .addResourceLocations("classpath:/templates/dist/webjars/");
 }
 }*/
